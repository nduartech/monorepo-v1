{
  "name": "monorepo",
  "packageManager": "yarn@4.1.1",
  "scripts": {
    "build-micros": "yarn workspace mr-solid run build && yarn workspace mr-solid run serve",
    "build-main": "yarn workspace mr-astro run build && yarn workspace mr-astro run preview"
  },
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {
    "esbuild": "^0.20.2",
    "postcss": "^8.4.38"
  },
  "devDependencies": {
    "postcss-sort-media-queries": "^5.2.0"
  }
}
