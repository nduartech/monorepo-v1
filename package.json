{
  "name": "monorepo",
  "packageManager": "yarn@4.2.1",
  "scripts": {
    "solidb": "yarn workspace mr-solid run build",
    "solidp": "yarn workspace mr-solid run serve",
    "astro": "yarn workspace mr-astro run dev"
  },
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {
    "esbuild": "^0.20.2",
    "postcss": "^8.4.38"
  },
  "devDependencies": {
    "postcss-sort-media-queries": "^5.2.0"
  }
}
